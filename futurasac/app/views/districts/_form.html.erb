<%= form_for(@district) do |f| %>
  <% if @district.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@district.errors.count, "error") %> prohibited this district from being saved:</h2>

      <ul>
      <% @district.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Descripción', {:class => "label_sistema"} %>
    <%= f.text_field :description, {:class => "input_text_sistema"} %>
  </div>
  <br>
  <div class="field">
    <%= f.label 'Estado', {:class => "label_sistema"} %>
    <%= f.check_box :status %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit '  Grabar  ', {:class => "input_bottom_sistema"} %>
  </div>
<% end %>
